#include <glob.h>
#include <fstream>
#include <cerrno>
#include <cstring>
#include <iostream>
#include <stdexcept>
#include <string>
#include <vector>
#include <cmath> 
#include <math.h>
#include <algorithm>
#include <unistd.h>
#include <memory>

using namespace std;







// // Typedefs
// typedef vector< vector< vector<float> > > Array;
// typedef vector< vector<float> > Matrix;
// typedef vector<float> Row;

// // Rad Specific Function Declarations
// void rad(const string& inputDirectory, bool train);
// Row computeHistogramRAD(Matrix& input, int frames, int index);
// Matrix calcDistances(Matrix& input);
// Matrix calcAngles(Matrix& input);


// // HJPD Specific Function Declarations
// void hjpd(const string& originalDirectory, bool train);
// Matrix jointPosDiff(Matrix& input, string coord);
// Row computeHistogramHOD(Matrix& input, int frames);


// // HOD Specific Function Declarations


// // Common function declarations
// vector<string> glob(const string& pattern);
// Matrix getData(string filename);
// Matrix checkNAN(Matrix data);
// string getcwd();


// float euclDist(float x1, float y1, float z1, float x2, float y2, float z2);
// float magnitude(vector<float> input);
// float dotProduct(vector<float> input1, vector<float> input2);







